{
  "name": "recipe-api",
  "version": "1.2.2",
  "description": "Node JS API for Recipes - migrated to Bun",
  "main": "index.js",
  "scripts": {
    "test": "bun test",
    "dev": "bun run start --stage local",
    "start": "bun run src/index.ts",
    "build:clean": "rm -rf ./dist && mkdir -p ./dist/logs",
    "build": "bun run build:clean && tsc --skipLibCheck"
  },
  "author": "Tom Shaw",
  "license": "ISC",
  "devDependencies": {
    "@types/supertest": "^6.0.2",
    "supertest": "^6.3.4",
    "typescript": "^5.4.4"
  },
  "dependencies": {
    "body-parser": "^1.20.1",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}
